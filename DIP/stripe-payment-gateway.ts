// import stripe


class StripePaymentGateway {
    async processPayment(amount: number, cardNumber: number, cvv: number, expiryDate: string) {
        try {
            // here will go the stripe payment
        } catch (error) {
            // here we will catch the errors
        }
    }
}